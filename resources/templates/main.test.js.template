{{#if features.babel}}
import message from '../src/main'
{{else}}
const message = require('../src/main')
{{/if}}

it('Test', {{#if features.babel}}() =>{{else}}function (){{/if}} {
  expect(message).toBe('Hello {{package.name}}!')
})
